hmily:
  server:
    configMode: local
    appName: demo-hmily
  #  如果server.configMode eq local 的时候才会读取到这里的配置信息.
  config:
    appName: demo-hmily
    serializer: kryo
    repository: mysql

repository:
  database:
    driverClassName: com.mysql.jdbc.Driver
    url : jdbc:mysql://localhost:3306/geek?characterEncoding=utf-8
    username: root
    password: root
  zookeeper:
    host: localhost:2181
    sessionTimeOut: 1000
    rootPath: /hmily
